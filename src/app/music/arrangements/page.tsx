// The nav points here, but the content is identical to the music
// home page (same pattern as physics/simulations/page.tsx re-exporting
// the physics home page). Kept as a separate file so /arrangements
// is a stable URL even if the home page's design changes later.
export { default } from "../page";
